{% extends 'base.html.twig' %}

{% block title %}Transport{% endblock %}

{% block body %}
    <h1>Transport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transport.id }}</td>
            </tr>
            <tr>
                <th>Type_transport</th>
                <td>{{ transport.typeTransport }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ transport.capacite }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ transport.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_kk_index') }}">back to list</a>

    <a href="{{ path('app_kk_edit', {'id': transport.id}) }}">edit</a>

    {{ include('kk/_delete_form.html.twig') }}
{% endblock %}
